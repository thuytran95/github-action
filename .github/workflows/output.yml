name: Output information
on: issues
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output github context
        run: |
          echo "${{ toJSON(github) }}"
          echo "${{toJSON(issues)}}"
